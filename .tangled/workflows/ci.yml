when:
  - event: ["push", "pull_request"]
    branch: main

engine: nixery

dependencies:
  nixpkgs/nixpkgs-unstable:
    - zig

steps:
  - name: Run tests
    command: |
      zig build test --summary all

